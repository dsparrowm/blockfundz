import { HowItWorks, Hero, PopularProducts, Services, SpecialOffer, Subscribe, SuperQuality, Footer} from '../sections';
import Nav from '../components/Nav';

const Home = () => {
  return (
    <main>
       
    </main>
  )
}

export default Home